# task_name
Площадки: страницы типов

# task_status
[x]

# task_init_description
Переделать раздел "площадки" так, чтобы у каждого типа площадки была своя страничка с понятной ссылкой (не через GET-параметр). Добавить задачу и выполнить.

# task_short_description
Сделать отдельные страницы типов площадок с человекочитаемыми URL.

# full_description
Нужно заменить фильтрацию типов площадок через query-параметр на отдельные страницы с нормальными URL (`/halls/<type>`). Навигация слева должна вести на отдельные страницы типов, а раздел площадок должен корректно открываться по прямой ссылке.

# План выполнения задачи
1. Обновить роуты и генерацию ссылок для типов площадок. (см. `docs/tasks/030/subtasks/step-001.md`)
2. Подстроить страницу площадок под новые URL и активный тип. (см. `docs/tasks/030/subtasks/step-002.md`)
3. Проверить сценарии переходов и прямого открытия страниц. (см. `docs/tasks/030/subtasks/step-003.md`)
