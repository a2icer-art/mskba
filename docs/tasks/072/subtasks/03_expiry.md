# 03_expiry.md
- В `event_bookings` добавлено поле `payment_due_at` (миграция).
- Добавлено поле `moderation_source` для фиксации источника смены статуса (manual/auto).
- При переводе в `awaiting_payment` рассчитывается `payment_due_at` по настройке площадки.
- Добавлен сервис авто‑отмены просроченных заявок с причиной «Вышел срок на оплату.».
