# Task 111 — Выполнение: Счётчики модерации (moderation pending badges)

task_name: Добавить счётчики модерации в header и sidebar (pending)

task_status: in-progress

task_init_description:
Добавить счётчики «модерация — в ожидании» в хедер (Панель управления) и в элементы сайдбара группы модерации, показывающие общее количество заявок в статусе `pending` и, при необходимости, количество по типам сущностей (`user`, `venue`, `venue_contract`).

task_short_description:
Подсчёт pending-запросов модерации в бекенде и проброс значений в Inertia navigation props; отображение бейджей в `MainHeader` и в сайдбаре для соответствующих пунктов.

full_description:
- Добавить подсчёт в `AdminNavigationService::getMenuGroups()` (total и по типам сущностей).
- Пробросить значения в `badge` полях элементов группы "Модерация" и как `meta.total_pending` в группе.
- Обновить фронтенд: `MainHeader.vue` и `MainHeaderNav.vue` для отображения бейджа рядом с "Панель управления" и в мобильном меню.
- Проверить права отображения: бейджи показываются только пользователям с доступом к модерации.

Plan:
1. Реализовать подсчёт и добавление `badge` в `AdminNavigationService`.
2. Обновить фронтенд (`MainHeader.vue`, `MainHeaderNav.vue`) для отображения бейджей.
3. Протестировать локально: авторизоваться как модератор/админ, проверить видимость бейджей в header и сайдбаре.
4. Добавить короткий коммит с описанием изменений и обновить задачу в `todo.md`.

Subtasks:
- backend: подсчёт pending
- frontend: отображение бейджа
- docs: пометить задачу выполненной после деплоя
